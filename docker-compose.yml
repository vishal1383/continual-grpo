services:
  experiment:
    build: .
    ipc: host
    shm_size: 32gb
    volumes:
      - ./outputs:/workspace/outputs
      - hf-cache:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
volumes:
  hf-cache:
